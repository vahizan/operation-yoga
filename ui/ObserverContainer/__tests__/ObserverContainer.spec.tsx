import { render } from "@testing-library/react";
import ObserverContainer from "../ObserverContainer";

describe("ObserverContainer", () => {
  it("should render without error", () => {
    render(<ObserverContainer />);
  });
});
